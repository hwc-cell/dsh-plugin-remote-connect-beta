# Security Policy

## 这个插件会做什么（先读这条）

本插件把一个**能在你机器上执行命令、读写文件**的 agent 暴露到网络。一旦被攻破，等同于把你的机器交出去。
请把它当作"对外发布一台可执行任意命令的主机"来对待，而不是一个普通网页工具。

作者**不提供**任何托管 / 中转服务；每个使用者自己准备服务器与域名，自己承担暴露后果。

## 两道必备门（+ 一道可选边缘门）

| 门 | 由谁提供 | 挡住什么 | 必开？ |
|---|---|---|---|
| 访问密钥 `?k=<随机>` → 签名 Cookie | 本插件（默认开启，不可关闭） | "知道地址就想进"的人；身份是 192 bit 的随机值，猜不出来 | **必开** |
| Harness 会话令牌 | 本插件注入（只对通过第一道门的请求） | 未持有会话的直连 | **必开** |
| 边缘口令（nginx `auth_basic` / Caddy `basic_auth` / Cloudflare Access / mTLS） | 服务器侧（`selfhost`）或隧道提供方 | 多一层与插件无关的独立门 | 可选，默认**不开** |

**为什么边缘门是可选的**：边缘口令是一条**共用**口令，无法按人吊销；而本插件的身份模型是「每人一条唯一
`?k=` 链接」。对单个使用者来说，这条共用口令在手机上是一长串要打的字，却挡不住任何已经拿到 `?k=` 的人，
所以 `snippets` 生成的模板默认把它注释掉。什么时候值得开：①同一台服务器/域名上还跑着别的东西，想让边缘统一兜底；
②你想再加一层**能按人吊销**的东西（`Cloudflare Access` / mTLS 比共用口令更合适）。

**凭据零落盘**：`?k=` 与边缘口令不得出现在任何日志、错误输出、遥测、shell history 或分享出去的截图里。
服务器侧必须为该子域做日志脱敏（`snippets` 生成的模板默认已脱敏）。

## 🔴 信任边界：你的服务器能看到什么（比门更要紧）

**TLS 在服务器上终止**，服务器到插件之间是回环上的明文 HTTP。所以：

- 服务器上拿到 root 的人（含 **VPS 服务商**、被攻破的其他服务）既能直连 `127.0.0.1:8788`，
  也能抓回环流量**看到 `?k=` 与登录 Cookie** —— 也就是说，服务器不可信时，插件这边加多少道门都不管用。
- 同一台机器上的**其他用户/进程**同样能连上那个回环端口（除非按下面第一条收紧）。

结论：**「你信得过这台服务器」是这套架构的地基**，这也是本项目不提供托管、要求每个使用者用自己的服务器的原因。
若要让服务器彻底看不到明文，只能把 TLS 终止挪到跑 Harness 的那台机器上（例如服务器侧用 `stream` 模块按 SNI
透传，证书装在 Harness 机器侧）——代价是服务器侧失去日志和边缘门能力。

两个能收紧的点：

1. **回环端口对同机其他用户可见** → 可把隧道从 TCP 口改成 Unix socket（`permitlisten` 用路径 + nginx
   `proxy_pass http://unix:/…`），只有该文件的属主/组连得到（挡得住同机其他用户，挡不住 root）；
2. **授权行必须带限制** → `restrict,remote-port-forwarding,permitlisten="127.0.0.1:8788"`
   （`dsh-remote keygen` 会直接打印这一行）。只贴裸公钥 = 那把钥匙变成「可登录 + 任意转发」，权限面完全不同。

## 紧急处置

1. 面板里**停止公网入口**（或 `dsh-remote` 进程直接 Ctrl-C）——隧道断开即公网不可达；
2. 面板里点一次**「换一个新口令」**——旧链接、旧二维码、已登录的浏览器立刻全部失效
   （口令是全机唯一的机器生成值，换掉的那个不会再被发出去）；
3. 开了边缘门的话，同时换掉那道口令；
4. 检查服务器 access log 是否曾把 `?k=` 落盘（`grep -c 'k=' /var/log/nginx/dsh.access.log`）；
5. 检查 Harness 会话里有无异常命令；
6. 如果怀疑**服务器本身**不可信（不是你自己的机器、或服务商可疑），换密钥不够 —— 先换服务器，再换密钥。

## 报告漏洞

请通过 GitHub Security Advisory（仓库 Security 标签页 → Report a vulnerability）私下报告，不要开公开 issue。
请在报告里给出：影响版本、复现步骤、影响面（能否绕过哪道门）。
48 小时内首次答复；确认后修复并致谢（除非你要求匿名）。

## 不在范围内

- 使用者自己把 `?k=` 链接或边缘口令公开分享导致的暴露；
- 使用者自己选的服务器（含 VPS 服务商）不可信导致的暴露 —— 见上面「信任边界」；
- Harness 本体（`deepseek-ai/deepseek-harness`）的漏洞——请报给上游。
